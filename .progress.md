# Modularize beacon_chain — Progress

## Current: Phases 5+6+7 launching in parallel

### Phases
- [x] Phase 1: Fix compile errors (committed)
- [x] Phase 2: ExecutionManager extraction (committed)
- [x] Phase 3: AttestationManager aggregation (committed)
- [x] Phase 4: Refactor attestation_verification (committed: 0116a04cd9)
- [~] **Phase 5: Block import → free functions** ← IN PROGRESS
  - Added `BlockImportContext` struct with 13 fields and `from_chain` constructor
  - Converted 9 private helper methods to free functions:
    - `import_block_update_validator_monitor` (uses ctx + chain for sync_committee_at_epoch)
    - `import_block_observe_attestations` (uses ctx.attestation_manager)
    - `import_block_update_slasher` (uses ctx.slasher)
    - `import_block_update_metrics_and_events` (uses ctx.slot_clock, block_times_cache, event_handler, config, light_client_server_tx)
    - `emit_sse_blob_sidecar_events` (uses ctx.event_handler, data_availability_checker)
    - `emit_sse_data_column_sidecar_events` (uses ctx.event_handler, data_availability_checker)
    - `check_blob_header_signature_and_slashability` (uses ctx + chain for verify_header_signature)
    - `check_data_column_sidecar_header_signature_and_slashability` (uses ctx + chain)
    - `handle_import_block_db_write_error` (uses ctx.attestation_manager, canonical_head, config, store, spec)
  - Public methods (`import_block`, `process_*`) construct `BlockImportContext::from_chain(self)` and pass to free functions
  - 2 free functions still take `&BeaconChain<T>` as extra param for calls to `sync_committee_at_epoch` and `verify_header_signature` (not extractable without touching other files)
- [ ] **Phase 6: Block production → free functions** ← LAUNCHING
- [~] **Phase 7: Execution orchestration → free functions** ← IN PROGRESS
  - Added `ExecutionOrchestrationContext` struct with `from_chain` constructor
  - Moved `block_is_known_to_fork_choice` to `ExecutionManager` as component method
  - Extracted `handle_invalid_justified_checkpoint` free function (shutdown logic)
  - Extracted `emit_payload_attributes_event` free function (SSE event emission)
  - Async methods (`process_invalid_execution_payload`, `prepare_beacon_proposer`,
    `update_execution_engine_forkchoice`) remain as `impl BeaconChain<T>` due to
    `self.clone()` / `spawn_blocking_handle` requirements, but delegate to free functions
- [ ] Phase 8: Remaining methods → move or delete
- [ ] Phase 9: Strip BeaconChain to ref-holder
- [ ] Phase 10: Final verification + cleanup

### Overall: ~40% complete (4/10 phases done, 3 launching in parallel)

### Last commit: `0116a04cd9` — Refactor attestation_verification to accept AttestationVerificationContext
### Tests: running (223/223 expected)
### beacon_chain.rs: ~2950 lines
