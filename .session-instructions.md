# Session Instructions: Modularize BeaconChain — Final Polish

## Read first
- `.claude/projects/.../memory/project_modularize_beacon_chain.md` — full state
- `beacon_node/beacon_chain/ARCHITECTURE.html` — the design doc
- This file

## Context
Branch `modularize-beacon-chain`, pushed to `jimmygchen/lighthouse` at commit `352486c6b8`. Code is DONE — Fulu 477/477, performance comparison done, all CI green. Only visual polish and coverage update remain.

## Tasks (in order)

### Task 1: "BeaconChain 2" hero animation
Title currently shows "BeaconChain, Modularised". Change to reveal effect:
- Show "BeaconChain" initially
- God object box swells and explodes (animation exists in CSS)
- Components emerge (animation exists)
- **"2"** fades in next to "BeaconChain" with a glow — iPhone product reveal style
- Subtitle stays: "From 200+ methods to zero. 7,317 lines to 126. Our tiniest BeaconChain, ever."

**Fix connection lines** (CRITICAL): Current lines are random CSS positioned divs that don't connect box-to-box. They look fake. Replace with SVG `<line>` elements or calculate CSS positions so lines connect actual component box centers. Must look like a clean architecture diagram.

### Task 2: Coverage numbers update
Build was running on `ssh root@204.168.250.92` (PID 546536).
- Check: `tail -3 /tmp/coverage-branch.log`
- If done: report at http://204.168.250.92:8082/ (server may need restart: `cd /root/coverage-branch-final/html && nohup python3 -m http.server 8082 --bind 0.0.0.0 &`)
- Extract new BlockImporter and DataAvailabilityMgr line coverage %
- Update the coverage comparison table in ARCHITECTURE.html
- Expected: BlockImporter ~75-80% (was 64%), DataAvailabilityMgr ~85% (was 60%)

### Task 3: ARCHITECTURE.md title
```
sed -i 's/BeaconChain, Refactored/BeaconChain, Modularised/' beacon_node/beacon_chain/ARCHITECTURE.md
```

### Task 4: Final review + push
- `cargo check --workspace --tests --release`
- `make lint`
- `git commit --no-gpg-sign`
- `git push jimmygchen modularize-beacon-chain`

## Remote VMs
- Kurtosis (branch): `ssh root@89.167.19.148` — 4 LH nodes running
- Kurtosis (unstable): `ssh root@204.168.250.92` — comparison testnet
- Coverage: `ssh root@204.168.250.92` — build may be done

## Rules
- Use subagents for ALL work, keep main agent free
- Don't run debug cargo checks — release only
- Commit with `--no-gpg-sign`, push to `jimmygchen`
- Don't block main agent on long builds
- Don't reduce scope — finish everything listed
