# Session Instructions: Modularize BeaconChain — Finish the POC

## Read first
- `.claude/projects/.../memory/project_modularize_beacon_chain.md` — full state
- `beacon_node/beacon_chain/ARCHITECTURE.md` — design principles (re-read periodically to avoid drift)
- This file

## Context
Branch `modularize-beacon-chain` in `worktrees/lighthouse-modularize-beacon-chain`. ~55 commits. `BeaconChain<T>` renamed to `BeaconComponents<T>` — pub-field-only struct with zero methods. 6 components, 4 context structs, Kurtosis testnet verified healthy. But CI is broken and cleanup remains.

## Tasks (in order — finish ALL changes before green CI)

### Task 1: Delete all `_from_chain` bridge functions
~30 `_from_chain` helper functions exist. Delete them. Inline the context construction at each call site:

```rust
// DELETE this helper:
fn block_import_context_from_chain(chain: &BeaconComponents<T>) -> BlockImportContext<T> { ... }

// INLINE at each call site:
let ctx = BlockImportContext {
    canonical_head: &chain.canonical_head,
    store: &chain.store,
    attestation_manager: &chain.attestation_manager,
    // ... explicit field access
};
```

Context structs STAY — they bundle deps for functions that need many params. The `_from_chain` helpers are the problem (lazy shortcut that hides deps).

Do NOT rename `chain` variable — that's future work.

### Task 2: Cleanup
- Verify zero `impl BeaconComponents<T>` blocks outside test_utils.rs
- Remove dead code, unused imports
- Ensure component tests pass and are meaningful (no trivial accessor tests)
- Check for any remaining `from_chain` patterns (method or function)

### Task 3: Capture Kurtosis performance metrics
Testnet running at `ssh root@89.167.19.148` (commit ae38443, expires 2026-04-18):
- Grep logs for block processing times
- Check attestation inclusion distance
- Check slot delays
- Count errors/warnings
- Save metrics to a file in the repo for the report

### Task 4: ARCHITECTURE.html
- Change title "Rearchitected" to "Refactored"
- Verify subtitle about correctness/performance/liveness is honest given Kurtosis metrics
- Add POC results section with ACCURATE metrics (unstable was 7317 lines, now 1025)

### Task 5: Green CI
Fix ALL compile errors across ALL crates. This is the final gate:
```bash
cargo check --workspace --tests
```
Known broken: ef_tests (fork_choice.rs), network (lookups.rs), slasher tests — they call removed methods. Update to use free functions or direct component access.

Ignore ONLY these pre-existing errors (not from our changes):
- `slot_clock::timestamp_now`
- `signature_sets::proposer_preferences_signature_set`
- `is_active_builder` / `can_builder_cover_bid` / `is_valid_proposal_slot`

### Task 6: Branch coverage report
On coverage VM `ssh root@204.168.250.92` (expires 2026-04-18):
- Check if branch build completed: `tail -5 /tmp/coverage-branch.log`
- If failed, re-run after Task 5 is done
- Serve on port 8082, report URL back
- Unstable coverage already at :8080 and :8081

## Future work (do NOT do in this session)
- Rename `chain` variable to `components` where it refers to BeaconComponents
- Replace context structs with caller-is-the-context pattern (NBP/HTTP handler passed directly)
- Promote component modules to separate crates
- Remove `Arc<BeaconComponents>` from callers — hold `Arc<Component>` directly

## Rules
- Use subagents for ALL work, keep main agent free
- Partition agents by FILE to avoid conflicts
- Commit without GPG (`--no-gpg-sign`), each message has rationale
- Run `cargo fmt --all` before commits
- Don't inflate metrics — be exact
- Re-read ARCHITECTURE.md if drifting
- Don't declare victory until `cargo check --workspace --tests` is green
